nota1 = float(input('Type the first note: '))
nota2 = float(input('Type the second note: '))
media = (nota1 + nota2) / 2
print('The first note is {:.1f}, the second note is {:.1f} and your average is {:.1f}.'.format(nota1, nota2, ((nota1 + nota2) / 2)))
